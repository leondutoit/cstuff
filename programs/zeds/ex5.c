#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[]) {
    // argc -> argument counr
    // array of char * strings for arguments

    int distance = 100;

    // this is also a comment
    printf("You are %d miles away.\n", distance);

    return 0; // exit code
}